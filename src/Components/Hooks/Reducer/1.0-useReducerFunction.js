export const useReducerFunction = ( stateList, action  ) => {

    switch ( action.type ) {
        case 'ADDtodo':
            
            return 'hello useReducerFunction';

       
    
        default:
            break;
    }
}